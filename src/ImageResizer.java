import javax.imageio.ImageIO;
import java.awt.image.BufferedImage;
import java.io.File;
import java.io.IOException;

public class ImageResizer {

    public static void resize(String path, int width){
        //TODO реализовать метод resize, который принимает путь до папки с изображениями и меняет их размер
//        File folder = new File(path);
//        if(!folder.exists() || folder.list().length == 0) throw new IllegalArgumentException();
//        File[] files = folder.listFiles();
//        for(var file:files){
//            try {
//                BufferedImage image = ImageIO.read(file);
//                image = Scalr.resize(image,Scalr.Mode.FIT_TO_WIDTH,width);
//                ImageIO.write(image,"jpg",file);
//            } catch (IOException e) {
//                throw new IllegalArgumentException();
//            }
//
//        }

   }
}